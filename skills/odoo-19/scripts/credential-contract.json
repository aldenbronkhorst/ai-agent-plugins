{
  "service": {
    "name": "Odoo",
    "aliases": ["odoo"]
  },
  "fields": {
    "ODOO_URL": {
      "required": true,
      "aliases": ["odoo url", "base url", "url", "urls", "website"]
    },
    "ODOO_API_KEY": {
      "required": true,
      "aliases": ["odoo api key", "api key", "api_key", "apikey", "access token", "token"]
    },
    "ODOO_DATABASE": {
      "required": false,
      "aliases": ["odoo database", "database name", "database", "db"]
    }
  }
}
